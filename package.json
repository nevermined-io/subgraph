{
  "name": "nevermined-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nevermined",
    "create-local": "graph create --node http://localhost:9020/ nevermined",
    "remove-local": "graph remove --node http://localhost:9020/ nevermined",
    "deploy-local": "graph deploy --node http://localhost:9020/ --ipfs http://localhost:5001 nevermined"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1",
    "@nevermined-io/contracts": "1.1.8"
  }
}