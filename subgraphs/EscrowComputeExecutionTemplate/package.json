{
  "name": "EscrowComputeExecutionTemplate",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined neverminedio/EscrowComputeExecutionTemplate",
    "create-local": "graph create --node http://localhost:8020/ neverminedio/EscrowComputeExecutionTemplate",
    "remove-local": "graph remove --node http://localhost:8020/ neverminedio/EscrowComputeExecutionTemplate",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 neverminedio/EscrowComputeExecutionTemplate"
  },
  "dependencies": {
    "@nevermined-io/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
